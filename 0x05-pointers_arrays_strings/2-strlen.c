#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: parameter to check
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int p;

	for (p = 0; s[p] != '\0'; ++p)
	{

	}

	return (p);
}
